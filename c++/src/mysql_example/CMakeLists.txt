CMAKE_MINIMUM_REQUIRED(VERSION 3.22)
PROJECT(mysql_example VERSION 1.0 DESCRIPTION "mysql_example project" LANGUAGES CXX C)
SET(CMAKE_CXX_STANDARD 14)

# 设置 C 编译器的编译选项
#set(CMAKE_C_FLAGS -Wall -O3)
# 设置 C++ 编译器的编译选项
#set(CMAKE_CXX_FLAGS
#        -static
#        -std=c++11 -Wall -O3 -Wl,-Bstatic
#        )

SET(MYSQL_LIB_DIR ${CPP_LIB_DIR}/mysql)
LINK_DIRECTORIES(${MYSQL_LIB_DIR})
#FIND_LIBRARY(LIB_MYSQL mysql ${MYSQL_LIB_DIR} NO_SYSTEM_ENVIRONMENT_PATH)
#SET(LIB_MYSQL ${MYSQL_LIB_DIR}/libmysql.lib)
#IF (EXISTS ${LIB_MYSQL})
#    MESSAGE(STATUS "LIB_MYSQL: ${LIB_MYSQL} EXISTS")
#ELSE ()
#    MESSAGE(FATAL_ERROR "LIB_MYSQL: ${LIB_MYSQL} NOT EXISTS")
#ENDIF ()
LINK_LIBRARIES(mysql)

OBJCT_TEMPLATE(${CMAKE_CURRENT_SOURCE_DIR})
